# Inventory example — copy to inventory.ini and fill in the real host.
# The ARM box is reached over SSH with the ***REMOVED*** key.
[arm]
***REMOVED*** ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/***REMOVED***

[all:vars]
ansible_python_interpreter=/usr/bin/python3
