# Inventory example — copy to inventory.ini and fill in the real host.
# The ARM box is reached over SSH with a dedicated deploy key.
[arm]
<arm-host-ip> ansible_user=<user> ansible_ssh_private_key_file=~/.ssh/<deploy-key>

[all:vars]
ansible_python_interpreter=/usr/bin/python3
